/*
parseInt('3.1415') will evaluate to 3 because parseInt returns a 
whole integer without decimal places. This is because parseInt stops returning 
numbers when it encounters a non-number (here, the decimal point).
*/