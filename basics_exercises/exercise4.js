/* console.log('5' + 10) logs 510 instead of 15 because '5' 
is a string, and not a number. When a string and
another data type are added with '+', the non-string undergoes implicit 
type coercion and is turned into a string. The strings are then concatenated 
into a single string.*/