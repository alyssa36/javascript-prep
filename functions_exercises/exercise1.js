/* 
This code logs 1 to the console because bar = 1. The foo function could have 
modified the value of bar (with "bar = 2"), but instead it created a new 
variable (also with the name 'bar') inside the function by using 'let'.
*/